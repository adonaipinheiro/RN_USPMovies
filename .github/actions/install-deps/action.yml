name: Install dependencies
description: Set up Node and install JS dependencies (with caching)

runs:
  using: composite
  steps:
    - name: Setup Node
      uses: actions/setup-node@v4
      with:
        node-version: 22
        cache: yarn

    - name: yarn install
      shell: bash
      run: yarn install --frozen-lockfile
