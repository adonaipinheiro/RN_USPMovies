// camada: presentation (navegação) — barril re-exportando o wrapper fino
// sobre o React Navigation (navigation.ts) e o coordinator (coordinator.ts).

export * from './navigation';
export * from './coordinator';
